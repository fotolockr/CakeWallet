import Foundation

struct AppSecrets {
    static let backupSalt = "{{ argument.backupSalt }}"
    static let keychainSalt = "{{ argument.keychainSalt }}"
    static let morphRef = "{{ argument.morphRef }}"
    static let changeNowApiKey = "{{ argument.changeNowApiKey }}"
}
